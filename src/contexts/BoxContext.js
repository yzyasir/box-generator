import {createContext} from 'react';

const BoxContext = createContext();
// this is all we have to do here, all we are doing is creating the outermost container
// making the context box, the container

export default BoxContext;